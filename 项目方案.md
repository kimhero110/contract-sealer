# 扫描合同盖章工具 —— 项目方案

> 版本：v1.3（第二轮工程评审修订版）+ v2.0 实施记录
> 日期：2026-09-03（v1.3）/ 2026-09-04（实施记录）
> 修订记录：v1.1 根据工程评审修正骑缝章几何模型（原方案两种模型互相矛盾）、压平导出升为 P0 强制、新增纸边校准、新增测试策略、印章库改存 %APPDATA%。
> v1.2 新增随机效果引擎（角度/分割/色度/蒙尘，种子化可复现）、手写签名支持、抠图算法泛化为任意墨色提取。
> v1.3 第二轮评审：修复输出文件同名覆盖、种子记录与 P2 日志的矛盾（种子落盘升为必备）、非 A4 件静默假定改为比例检测强制校准、新增最小切片宽度约束、明确混尺寸页面 y 度量、微调步进改物理单位、补充拼合对齐说明、新增宽度守恒属性测试。

## 1. 项目背景与目标

日常工作中，经常需要把公章盖到**扫描版合同**（PDF 或图片文件）上，例如合同最后一页的落款处盖章、整本合同的**骑缝章**。手工用 PS 处理效率低、比例不统一、骑缝章切割麻烦。

**目标**：做一个本地桌面工具，让单人用户能够：

1. 打开扫描合同（PDF / JPG / PNG），逐页预览；
2. 把印章图片拖放到页面任意位置，可缩放、旋转、调透明度，**按真实公章物理尺寸校准比例**；
3. 一键给整本合同加**侧边骑缝章**（印章按页数自动切割，跨页拼合为完整印章）；
4. 输出新的 PDF / 图片文件，不破坏原始文件。

**非目标（本期不做）**：多人协作、审批流、CA 数字签名/法律效力电子签章、云端存储。

## 2. 用户与使用场景

- **用户**：公司内部单岗人员（财务/法务/行政），Windows 环境。
- **典型流程**：
  1. 收到对方回传的扫描合同 PDF（或手机拍照的 JPG）；
  2. 打开工具 → 拖入合同 → 选择已配置的"公章"；
  3. 在末页落款处拖放盖章，微调大小（默认已是真实尺寸）；
  4. 点击"骑缝章"→ 选择边距和页范围 → 预览确认；
  5. 导出 `xxx_已盖章.pdf`，归档。

## 3. 功能需求

### 3.1 文件支持

| 功能 | 说明 | 优先级 |
|---|---|---|
| PDF 导入/预览 | 逐页渲染预览，支持缩放、翻页 | P0 |
| 图片导入 | JPG/PNG/BMP/TIFF，多图按文件名排序合成文档 | P0 |
| 导出 PDF | 盖章结果合成新 PDF，分辨率不低于原扫描件 | P0 |
| 导出图片 | 单页导出 PNG/JPG | P1 |
| 批量处理 | 文件夹内多个合同套用同一盖章规则 | P2 |

### 3.2 印章与签名管理

| 功能 | 说明 | 优先级 |
|---|---|---|
| 印章导入 | 上传扫描/拍照的印章图（白底） | P0 |
| 自动抠图 | 任意墨色提取（红章/蓝黑签名通用）：去背景、保留墨迹浓淡 | P0 |
| 手写签名 | 上传白纸签名照片，抠图后入**签名库**（与印章库并列），记录常用宽度（如 35mm），盖章流程与印章完全一致 | P0 |
| 印章库/签名库 | 保存多枚印章（公章/合同章/法人章/骑缝专用章）与多个签名（不同人员），含默认尺寸 | P0 |
| 尺寸校准 | 录入印章真实物理直径（如公章 40mm/42mm），盖章时按页面物理尺寸换算 | P0 |
| 手动修正 | 手动框选墨迹区域、旋转扶正、调透明度/颜色深浅 | P1 |
| 程序生成印章 | 输入公司名称生成圆形章（备用方案） | P3 |

### 3.3 盖章（普通章与签名）

- 预览页上**拖放**印章或签名，支持鼠标缩放、旋转、微调位置（方向键步进 **0.1mm**，Shift+方向键 1mm，均为物理单位，与预览缩放无关）；
- 印章默认按**物理尺寸 1:1** 落章（见 §4.3 比例换算），可在此基础上 ±50% 缩放；
- **缩放/旋转时实时显示当前实际物理尺寸**（如"⌀ 38.2mm" / "宽 34.5mm"），防止用户无意中偏离真实尺寸；
- 透明度、混合模式（正片叠底，模拟印泥/墨迹压在纸上的效果）；
- **随机效果**：每次落章的角度、色度、蒙尘带小幅度随机（默认开启，见 §4.9），每次导出的章迹都略有不同，更像真章；
- 同一位置可记忆为"模板"（如"末页落款处：公章+法人章+签名"），下次一键复用（P1）；
- 一页可盖多枚章与签名组合（公章 + 法人章 + 经办人签名）。

### 3.4 骑缝章（核心难点）

- **侧边骑缝章**：用户选定页范围（默认全部页）和侧边（默认右侧）；
- 采用**切片平铺模型**（评审修正，见 §4.4）：印章按页数 N 竖向切割为 N 份，第 i 页的切片**完整落在页面内**、贴齐纸边（内缩 1–2mm），N 页切片横向平移拼合即还原完整印章；
- **分割随机化**（v1.2）：切片宽度不均匀（默认 ±20% 随机），各切片宽度之和恒等于印章总宽——每页条带宽窄不一，观感接近真实扇形摊开盖章，而拼合时按实际宽度顺序平铺依然完美还原；
- 拼接校验：提供"拼合预览"模式，按真实导出结果把所有页的骑缝条带按顺序拼在一起，确认能还原完整印章；
- 参数：印章直径、切片内缩距离、垂直位置（上/中/下或自定义）、整体旋转角、随机强度；
- 支持**双骑缝**（左右两侧各一枚，P2）。

### 3.5 输出与安全

- **只生成新文件，绝不覆盖任何已有文件**（原件与既往输出一视同仁）：默认文件名 `{原名}_已盖章_{日期}_{时分秒}.pdf`，重名再追加序号；
- **压平导出为 P0 且默认强制**（评审修正）：导出时印章与页面栅格化合并为一张图像，PDF 中不存在独立印章图片对象——防止他人用 pdfimages 等工具从 PDF 里提取出干净的透明印章 PNG 复用。本项目处理的都是扫描件，压平不损失文字可选择性；
- 输出分辨率 ≥ 原扫描分辨率（默认 300 DPI）；
- 导出先写临时文件、完成后原子改名，崩溃/中断不产生损坏的输出文件；
- **种子落盘为 P0 必备**（二轮评审修正）：每次导出在输出文件旁写入同名 `.sealog`（JSON：随机种子、页范围、盖章位置、随机参数快照），保证任何一次导出可复现、可回溯；完整的导出历史日志（检索/列表界面）仍为 P2。

## 4. 技术方案

### 4.1 技术选型

| 层 | 选型 | 理由 |
|---|---|---|
| 语言 | Python 3.11+ | 生态成熟，图像/PDF 库齐全，开发快 |
| GUI | PySide6 (Qt) | 原生 Windows 体验，拖拽/画布/多页预览好用，可打包单 exe |
| PDF 渲染/写入 | PyMuPDF (fitz) | 渲染快，支持在页面上精确叠加图像、控制物理坐标 |
| 图像处理 | OpenCV + Pillow + numpy | 印章抠图（颜色分割）、旋转缩放、透明度合成 |
| 打包 | PyInstaller（或 Nuitka） | 单文件 exe，免安装分发 |

备选：Electron + pdf-lib（若后续要跨平台/Web 化再考虑，本期不选）。

### 4.2 模块划分

```
contract-sealer/
├── app/                    # PySide6 界面层
│   ├── main_window.py      # 主窗口：左侧页缩略图 / 中间画布 / 右侧印章与参数面板
│   ├── canvas.py           # 页面画布：渲染页面 + 印章拖拽/缩放/旋转交互
│   └── seal_panel.py       # 印章库管理、骑缝章参数面板
├── core/
│   ├── document.py         # 文档模型：统一抽象 PDF/图片为"页列表"(尺寸DPI+图像)
│   ├── seal.py             # 印章/签名模型：抠图、缓存、物理尺寸元数据
│   ├── extract.py          # 墨迹提取：红章/深色墨迹分割→去噪→透明PNG
│   ├── stamp.py            # 普通盖章/签名合成
│   ├── perforation.py      # 骑缝章：随机宽度切割算法 + 逐页放置
│   ├── randomize.py        # 随机效果引擎：角度/色度/蒙尘/分割，种子化纯函数
│   └── export.py           # 压平导出 PDF/图片（临时文件+原子改名）
├── tests/
└── assets/
```

关键设计：**UI 层只操作"页面坐标"，core 层负责物理坐标换算**，预览缩放不影响落章精度。

### 4.3 公章比例换算（物理尺寸 1:1）

真实公章有标准物理尺寸（常见直径：公章/合同章 40mm 或 42mm，法人章 18–20mm）。为了让屏幕上的章"盖出来和真的一样大"：

1. 印章入库时录入**真实直径 D mm**（可用尺子量扫描件上的章，或直接填标准值）；
2. 目标页面的物理尺寸由 PDF page box 得到（点 point 单位，1pt = 1/72 inch）；
3. 印章在页面上的像素尺寸 = `D / 25.4 * DPI`，其中 DPI 为页面栅格化分辨率（默认 300）；
   - 例：40mm 章 @300DPI → `40/25.4*300 ≈ 472 px`；
4. 对图片型扫描件及**页面框不可信的 PDF**（手机拍照转 PDF、带黑边/裁切/倾斜的扫描件），页面框 ≠ 真实纸边，按页面框换算会静默盖错尺寸。提供三级校准（评审新增）：
   - **比例检测前置**（二轮评审修正）：打开文件时检测页面长宽比，偏离 A 系纸 √2±5% 时**弹出提示要求校准**，不允许静默按 A4 假定盖错（A5/Letter/长条收据都会触发）；比例正常时默认按 A4 处理、可随时手动改；
   - **纸边校准（M1，手动）**：用户在页面上框选纸面四角/边缘，以纸边区域为物理尺寸基准（"A4 纸的宽边是 210mm"）；
   - 自动纸边检测（M3，OpenCV 边缘检测+透视变换，同时可顺带做纠偏）；
5. 预览缩放只影响显示，导出时始终按物理尺寸换算 → **保证打印出来大小正确**。

### 4.4 骑缝章切割算法（v1.1 评审重写）

> v1.0 草案在这里犯了错：同时写了"切片平铺"和"圆心压纸边线半章悬出"两种互相矛盾的几何模型，且每条切片独立随机旋转会破坏拼合验证。以下为修正后的唯一定义。

**几何模型：切片平铺。** 每条切片完整落在页面内，右侧贴齐纸边（内缩 inset，默认 1.5mm）；N 页的切片横向平移拼接后还原完整印章。不做"半章悬出纸外"——纸外像素导出即被裁掉，对拼合验证没有任何贡献。

输入：印章透明 PNG（物理直径 D mm）、**选中页范围内的页数 N**（非文档总页数）、侧边（左/右）、内缩 inset mm、垂直位置 y、整体旋转角 θ（默认 0）、随机种子 seed。

1. 按目标 DPI 计算印章像素宽 `W = D/25.4*DPI`，将印章重采样到该尺寸；
2. **随机宽度切分**（v1.2）：由 seed 驱动生成 N 个宽度权重（默认均匀值 ±20% 抖动），归一化使各切片宽度之和恒等于 W；宽度随机**不影响可拼合性**——拼合时按实际宽度顺序平铺，数学上必然还原完整印章。随机强度为 0 时退化为等宽切分（N=1 时退化为普通靠边章）；
3. **最小切片宽度约束**（二轮评审新增）：若 `W/N < 1.5mm`（切片过窄，打印不可见、骑缝验证失效），**明确提示**用户缩小页范围或加大印章直径，禁止静默产出无效骑缝章；
4. 第 i 页（i 从 0 开始，相对选中页范围）放置第 i 条：
   - 右侧骑缝：切片右边缘位于 `纸边x - inset`；切片间页序映射固定为"页码升序 = 切片从左到右"，**不允许**再出现"方向约定含糊、以预览为准"的说法——方向由拼合预览**验证**，不是由预览**决定**；
   - 左侧骑缝：镜像处理；
   - **垂直位置 y 按各页纸边独立度量**（如"距页顶 100mm"在每页各自计算），正确处理合同里夹 A3 附图等混尺寸页面；
5. **逐页微抖动**（v1.2，seed 驱动）：每条切片可有独立的微小垂直偏移（默认 ±0.5mm）和旋转（默认 ±1°），模拟真实盖章时每页受力差异。代价是拼合不再是像素级完美——这正是真实骑缝章的状态，因此：
   - 拼合预览永远渲染**真实导出结果**（所见即所得），用户肉眼确认印文连续可辨；
   - 自动化断言改用带容差的图像相似度（SSIM 阈值），不再用逐像素相等；
   - 抖动幅度有硬上限（垂直 ≤ 1mm、旋转 ≤ 2°），防止调参过度导致拼合不可辨；
6. 放置坐标全部走 core 层物理坐标换算，与预览缩放无关；
7. "拼合预览"：直接读取**即将导出的真实结果**，把各页条带按页序排列、**以各切片实际宽度累积定位**（页 i 的条带左移 `sum(w_j, j>i)`，等宽切分是其特例）横向拼接渲染，用户确认无误后才允许导出。

> 说明：骑缝章的法律意义在于"多页拼接能还原完整印文"，因此切割+页序映射必须可验证，拼合预览是必备功能而非锦上添花。拼合预览同时也是自动化测试的 golden-image 断言对象（见 §4.7）。

### 4.5 墨迹自动抠图（v1.2 泛化）

印章是红色，签名是蓝/黑色，抠图算法从"红色分割"泛化为**任意墨色提取**：

1. **红色印文**（印章）：转 HSV，按红色区间（H≈0–15 及 160–180）阈值分割；
2. **深色墨迹**（签名）：按"低亮度 + 相对背景的高对比度"分割（纸白墨黑/蓝），蓝色签名辅以蓝色通道差分增强；
3. 形态学去噪 + 连通域过滤（去掉纸面噪点）；
4. 墨迹浓度 alpha 化：墨色越浓 alpha 越高 → 得到带透明通道、保留印泥/笔迹浓淡的 PNG；
5. 白底/灰底照片先做亮度均衡；提供"抠图强度"滑杆和手动框选兜底；
6. 结果入库（PNG + 元数据 JSON：名称、类型[印章/签名]、真实直径或宽度、创建时间）。

### 4.6 数据与文件安全

- 全程本地运行，无任何网络请求；
- 只读打开原文件，导出写新文件；导出先写临时文件、完成后原子改名；
- **印章库存放位置：`%APPDATA%\contract-sealer\seals\`**（评审修正）。严禁放 exe 相对目录——exe 很可能放在局域网共享盘上，印章图会裸奔给整个内网；
- 印章图片是本工具的"敏感资产"，首次启动时明确提示用户其存放位置与保管责任（后续版本可考虑加密存储，P3）。

### 4.7 测试策略（v1.1 评审新增）

core 层全部算法脱离 GUI 可测，用 pytest 覆盖；测试素材直接用项目目录里现成的扫描件（`1.jpg` 等）和印章 PNG 做 fixture。

| 测试层 | 内容 | 断言方式 |
|---|---|---|
| 切割-重组 | 印章切成 N 条（含随机宽度）后按页序拼接 | 固定种子 → golden-image 像素差/SSIM 阈值断言 |
| **宽度守恒**（二轮新增） | 随机取 100 个种子做切分 | 属性断言：每次切分宽度之和 = W，且每片 ≥ 最小宽度约束 |
| 页序映射 | 第 i 页拿到的切片内容 | 与同种子手动切割的期望切片对比 |
| 随机引擎 | 同种子两次运行结果一致；不同种子结果不同；各维度不越硬上限 | 数值断言（种子可复现性 + 幅度边界） |
| 抠图 | 红章样张、蓝/黑签名样张分别提取 | 墨迹区域覆盖率、背景残留率阈值断言 |
| 尺寸换算 | `mm→px`、A4 假定、纸边校准三种路径 | 数值断言（40mm @300DPI = 472px ±1） |
| 导出 | 导出 PDF 后栅格化，提取墨迹区域测直径/宽度 | 物理尺寸误差 ≤ 1mm（与验收标准 1 对齐） |
| 安全 | 导出 PDF 中枚举图片对象 | 断言不存在可分离的印章/签名图片对象（压平生效） |
| 原子写 | 模拟导出中断 | 原文件不变、无残留损坏输出 |

GUI 层不做自动化测试，靠里程碑人工验收；但所有落章逻辑必须可绕过 GUI 直接调用（§4.2 的 UI/core 分层就是为此服务）。

### 4.9 随机效果引擎（v1.2 新增）

真实盖章每次都不一样：角度歪一点、印泥浓淡不一、有缺油的小缺口。完全标准化的数字章反而"假"。本模块给普通章、骑缝章、签名提供统一的随机化，核心原则是**种子化、有界、所见即所得**。

**种子化随机（seeded RNG）**：
- 每次导出操作生成一个随机种子，该次导出的所有随机效果由它驱动；
- 种子记入导出日志 → 同一份合同可以复现同一套"手感"，出问题可回溯；
- 自动化测试使用固定种子 → 输出确定、可断言；
- **种子随每次导出落盘**（输出文件旁的 `.sealog`，见 §3.5），不依赖 P2 的完整日志功能；
- 预览与导出共用同一次采样的结果，**严禁预览一套、导出另一套**。

**四个随机维度**（均有强度滑杆，0 = 完全关闭，默认开启小幅度）：

| 维度 | 默认幅度 | 硬上限 | 说明 |
|---|---|---|---|
| 角度 | ±2° | ±5° | 整章绕中心随机旋转 |
| 色度（印泥浓淡） | ±10% 明度/饱和度 | ±25% | 模拟蘸印泥多少的差异；签名同样适用 |
| 蒙尘（印泥不均） | 轻微 | 中等 | 在墨迹 alpha 上叠加低频噪声，产生"缺油"的小缺口和边缘磨损；不得影响印文可辨识度（噪声只减不增，幅度有界） |
| 骑缝分割 | 切片宽度 ±20% | ±40% | 见 §4.4，宽度之和恒等，拼合性不受影响；另有逐页垂直 ±0.5mm / 旋转 ±1° 的独立微抖动（硬上限 1mm / 2°） |

**实现位置**：`core/randomize.py`，输入（图像、参数、种子）→ 输出变换后图像，纯函数、无状态，GUI 与批量导出共用。蒙尘噪声用 numpy 生成低频 Perlin 式噪声图调制 alpha，计算量可忽略。

### 4.10 手写签名管线（v1.2 新增）

签名 = "异形印章"，复用整条盖章管线，差异仅在三点：

1. **入库**：上传白纸签名照片 → §4.5 深色墨迹分割抠图 → 录入**常用宽度**（默认 35mm，签名看宽度不看直径）；
2. **落章**：与印章相同的拖放/缩放/旋转/正片叠底/实时毫米显示；签名通常与印章组合出现在落款处（章压住签名一角是常见真实样式，支持调整叠放顺序）；
3. **随机效果**：角度/色度/蒙尘全部适用；签名不参与骑缝切割。

法律提示：本工具生成的签名图样仅用于内部流程文件，README 中声明不得用于伪造他人签名。

### 4.11 许可证说明（v1.1 评审新增）

PyMuPDF 采用 AGPL 许可证。本项目为**公司内部自用工具、不对外分发**，AGPL 合规无问题；但文档与 README 必须写明"不得将打包产物对外销售/分发"，如需对外分发再评估换用 pypdf+渲染分离方案或购买商业许可。

## 5. 里程碑计划

| 阶段 | 内容 | 产出 |
|---|---|---|
| M1 基础打通（约 1.5 周） | PDF/图片导入预览、墨迹抠图（章+签名）、单页拖拽盖章/签名、物理尺寸换算（含手动纸边校准）、随机效果引擎（角度/色度/蒙尘）、压平导出 PDF、对应测试 | 可用 MVP |
| M2 骑缝章（约 1 周） | 随机宽度切片平铺算法、逐页放置（含微抖动）、拼合预览、参数面板、切割-重组 golden-image 测试 | 核心功能完整 |
| M3 体验打磨（约 1 周） | 印章库/签名库管理、落款组合模板、批量处理、自动纸边检测、**页面旋转**（M1 真实素材发现：扫描件可能上下颠倒）、打包 exe | 可交付日常使用 |

## 6. 风险与应对

| 风险 | 应对 |
|---|---|
| 扫描件页面框 ≠ 真实纸边（拍照件/带黑边/倾斜），章大小静默失真 | 三级校准：A4 假定 → 手动纸边校准（M1）→ 自动纸边检测（M3） |
| 印章照片质量差（阴影/反光）导致抠图不干净 | 手动框选 + 强度滑杆兜底；必要时引导重新拍摄 |
| PDF 有加密/权限限制无法写入 | 打开时检测并提示；尝试只读渲染后整页栅格化再盖章 |
| 骑缝章页序/方向错误 | 页序映射代码固定 + 拼合预览强制确认 + golden-image 自动化测试三重防线 |
| 随机效果过度导致印文不可辨或拼合失败 | 所有维度设硬上限；拼合断言用带容差 SSIM；预览与导出同源（所见即所得）；滑杆可归零 |
| 页数过多导致切片过窄、骑缝失效 | 最小切片宽度 1.5mm 约束，触发时明确提示而非静默导出 |
| 输出文件同名覆盖（同日重复盖章） | 文件名精确到时分秒 + 重名追加序号，任何已有文件都不被覆盖 |
| 非 A4 件（A5/Letter/收据）按 A4 假定静默盖错 | 打开时比例检测，偏离 √2±5% 强制提示校准 |
| 干净印章/签名图被从导出 PDF 中提取复用 | 压平导出默认强制（P0），导出测试断言无可分离墨迹对象 |
| 印章库泄露（exe 放共享盘场景） | 印章库存本机 %APPDATA%，首次启动提示保管责任 |
| 法律风险（伪造印章滥用） | 仅限本单位已授权印章的内部使用，README 中声明 |

## 7. 验收标准

1. 打开一份 20 页扫描 PDF，末页盖章导出后，打印测量印章直径误差 ≤ 1mm（自动化测试同步断言栅格化直径）；
2. 骑缝章：取导出 PDF 任意相邻两页的印章条带，拼合后印文连续可辨（带容差的 SSIM 测试 + 拼合预览人工确认；随机抖动在硬上限内不得影响可辨识度）；
3. 全程不修改原始文件；崩溃/中断不产生损坏的输出文件（先写临时文件再改名，有对应测试）；
4. 导出的 PDF 中**无法提取出独立的印章/签名图片对象**（压平默认强制，有对应测试）；
5. 随机效果：同一合同用同一种子可复现导出结果；所有随机维度不越硬上限（有对应测试）；
6. 签名：上传照片抠图后落章，**按设定宽度落章、导出栅格化测量误差 ≤ 1mm**，与印章组合叠放顺序正确；
7. 大页数边界：当印章直径/页数使切片宽 < 1.5mm 时，工具明确提示而非静默导出（有对应测试）；
8. §4.7 核心算法测试全部通过（含宽度守恒属性测试）；
9. 单文件 exe 双击即用，无需安装 Python 环境。

## 8. v2.0 实施记录（2026-09-04）：方案落地后的设计反转与增补

方案是地图，实施是路况。以下决策在开发过程中**推翻或超越了 v1.x 的设计**，以本文为准：

### 8.1 废除"每次导出新种子"（推翻 §4.9 部分设计）

v1.2 设计"每次导出重采样所有随机效果"。实际使用发现与"所见即所得"根本冲突：
画布上看到的章和导出的章不是同一个。**v0.2.1 起改为：落章时采样一次，全程复用同一份
processed 图像**；想要新手感点「换一批手感」主动重摇（仅普通章；骑缝切片因未保存
PerforationSpec 无法重摇，需重新打开对话框——已知限制）。

### 8.2 旋转符号约定（血泪教训）

画布 Qt `setRotation` 正角度=顺时针，导出 PIL `rotate` 正角度=逆时针。
用户在预览拧正 θ，导出净误差 2θ（90° 修正变 180° 倒立）。**约定：全系统
rotation_deg 一律顺时针为正（Qt 语义），导出侧取负适配 PIL**。测试
`test_rotation_direction.py` 用不对称箭头守着这个约定。

### 8.3 骑缝章自动贴合纸边（增补 §4.4）

扫描件图像边缘 ≠ 纸张边缘（白边/灰边），固定 inset 会产生视觉留空。
`detect_paper_edge_x_px` 逐页检测真实纸边（列亮度剖面 + 渐晕容差），切片贴合
检测边而非图像边；全幅扫描回退图像边。默认 inset 1.5→0.5mm。

### 8.4 页面懒加载（增补 §4.2，P0 内存修复）

v1.x 的 Document 打开即全量渲染 300DPI 全部页面（50 页 ≈ 1.25GB，会翻车）。
v0.3.0 起 Page 懒加载：元数据先行、首次访问才渲染、Document 级 LRU（驻留 ≤6 页）、
变异（旋转/校准）后脱离缓存永久驻留、缩略图走独立低 DPI 渲染。

### 8.5 撤销栈（增补，P1）

删除章/删除整组/骑缝组微调/页面旋转/四点校准/移动章均支持 Ctrl+Z（命令栈上限 50）。
教训级细节：撤销只改记录，**画布重建必须跳过"画布→记录"回同步**，否则旧贴图位置
会把刚撤销的记录覆盖回去（与 v0.2.1 的差集剪除 bug 同源：画布和记录谁是真相源的问题）。

### 8.6 实测抓获的 bug 清单（供后来者参考）

- transformOriginPoint 像素值混入毫米坐标 → 章画到页外 372mm（"点击毫无变化"真凶）；
- 差集剪除误删未渲染记录 → 当前页骑缝/模板丢失；
- QSlider() 默认竖向 → 随机强度滑杆从出厂就是小方块；
- Gitee releases/tags 对缺失发行版返回 200+null 而非 404（CI 幂等逻辑被坑）。
