# CI 与打包用的锁定版本。裸包名 pip install 会让每次 tag 构建拿到不同的
# 上游版本——出问题时没人知道昨天装的是哪一版。
#
# 刷新方式：本地升级、跑通 pytest、再把新版本号写回这里并提交。
# 下面标注"已验证"的版本是当前测试跑通过的组合。
PyMuPDF==1.26.0                  # 已验证
opencv-python-headless==5.0.0.93 # 已验证
Pillow==12.2.0                   # 已验证（>=10.1 才有 load_default(size=)）
numpy==2.2.6                     # 已验证
PySide6==6.9.1                   # GUI 层，仅在有 Qt 的平台上装
