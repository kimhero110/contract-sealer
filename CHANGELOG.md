# 更新日志

本项目以 [GNU AGPL-3.0](LICENSE) 发布。

## v0.5.0

### ⚠️ 本版本重写了 Git 历史

为了把误提交的构建产物从仓库里彻底清掉，**v0.5.0 重写了 main 分支的提交历史**。
如果你在此之前克隆过本仓库，`git pull` 会失败或产生一堆重复提交。请改用：

```bat
git fetch origin
git reset --hard origin/main
```

有本地改动的话，先 `git stash`、reset 之后再 `git stash pop`。
最省事的办法是删掉旧目录重新克隆一次。

**为什么必须这么做。** 提交 `5feb1d5`（"fix: 全量猎虫第二轮"）误把 PyInstaller 的
输出目录 `_internal/` 一起提交了，其中 `_internal/cv2/cv2.pyd` 单文件 **85MB**。
后果不只是"仓库大"：

- `.gitignore` 当时没有覆盖 `_internal/`、`dist/`、`build/`，同类事故随时会再发生；
- 5700 行源码的仓库，克隆要拉 38MB，其中绝大部分是一个**没有任何人审过的 Windows 二进制**；
- Git 的对象是不可变的。仅仅 `git rm` 只是让它在最新提交里消失，blob 依旧躺在历史里，
  克隆照拉不误。**唯一的清除办法就是重写历史**，代价就是上面那次 reset。

现在 `_internal/`、`dist/`、`build/`、`*.egg-info/` 都已进入 `.gitignore`，
构建产物不会再进仓库。工作区体积从 93MB 降到约 10MB。

### 修复

- **左侧骑缝章的页序映射未镜像**，扇开拼出来的印文左右颠倒。位置做了镜像、页序没做。
  正确定义要从验章动作倒推：左开口往左扇开时，从左到右看到的是最后一页到第一页，
  所以切片必须反着发。同时拼合预览改为按切片在印章内的序号排序而非按页序——
  左开口时两者相反，旧预览会显示一张左右颠倒的印文，而用户正是拿它确认"拼得回去"的。
- **骑缝章旋转抖动破坏宽度守恒**。PIL 的 `rotate(expand=True)` 把切片撑宽
  `|w·cosθ| + |h·sinθ|`，40mm 章切 5 页、抖动 1° 时每片多出约 0.7mm。
  现在旋转后居中裁回配额宽度。
- **四点纸边校准必崩**。`_on_points_picked` 在赋值前引用局部变量 `recs`，
  每次点完四个角都抛 `UnboundLocalError`。
- **印章库会静默覆盖**。`Seal.save` 不检查同名，"公章(1)" 与 "公章 1" 转义后撞同一个
  文件名。现在默认拒绝覆盖，导入时给出"覆盖／自动改名／取消"三选。
- **删页后缓存串图**。页面 LRU 的键从 `id(page)` 改为单调递增的 `uid`。
- **非 Windows 上的数据目录不对**。印章库/模板/崩溃日志曾一律退到 `~/AppData/Roaming`，
  现在按平台约定分流。
- 补 AGPL-3.0 LICENSE 文件。README 原先把 AGPL 理解成"卖钱才受限"，实际触发条件是
  **分发**：把程序（含 exe）给到本组织之外就必须一并提供对应源码并沿用 AGPL。
- README 去掉"印章无法被从中提取"的过度承诺。压平只消除可分离的图层对象，
  防的是顺手一拖就复用，不防裁图。同时补上导出件会失去文字层这个此前未说明的取舍。
- 版本号此前在 pyproject、`core.__version__`、CI job 名三处各说各话，现已统一并加测试守住。

### 新增

- **四点校准改为拖把手**。进入校准时先用 OpenCV 自动框出纸面作为初值，检测不出来就
  给一个页面内缩的默认框，用户永远不必从零点四下。四个角是可拖把手，随时能改；
  拖动时放大镜贴着把手走、松开才定稿；实时显示框选长宽比并提示是否接近 A 系纸；
  选中把手后方向键微调 0.1mm、Shift 1mm；回车确认、Esc 取消、Tab 轮转四角。

  旧的"点四下"模型有三处劝退：点了就定死，错一个角只能整个重来；单击自带一两像素
  抖动，而放大镜是六倍，你在放大镜里看到没对准时点已经落下了；最要命的是取点期间
  `dragMode` 被设成 `NoDrag`，能缩放却不能平移，放大到看得清纸角后根本挪不到下一个角。
  把手模型沿用画布原本的 `ScrollHandDrag`：拖把手是调整，拖空白处是平移。

  顺带修掉一个视觉误导：旧版连线按点击顺序画，按对角顺序点四下会画出一个交叉的
  蝴蝶形，看着像出错了其实结果是对的。

- **输出目录可选**。导出走另存为对话框，选过的目录会记住，下次默认沿用；
  菜单里可一键恢复"与源文件同目录"。批量导出单独指定输出目录。
- **加盖日期**（Ctrl+D）。默认系统当天，日期、格式、字高都可改，走与印章相同的
  物理尺寸管线。不加随机手感；缺中文字体时明确报错而不是画一排豆腐块。
- **撤销 / 重做**（Ctrl+Z / Ctrl+Y）与**重盖**（Ctrl+R）。撤销栈重写为快照式命令栈，
  动作中途失败会整体回滚。
- **待处理文档管理**。追加导入、页列表多选删页（可撤销）、关闭当前文档。

### 工程

- 依赖版本锁进 `requirements.txt`，CI 不再裸 `pip install`。
- CI 新增 ruff 检查，以及一个不装 Qt 的 Linux 核心测试 job。
- 主窗口拆出 `app/dialogs.py` 与 `app/imageutil.py`。
- 批量导出加进度条，可中途取消。
- 没装 PySide6 的机器上 GUI 用例自动 skip，core 层算法测试照跑。
- 测试从 89 个增加到 138 个。

### 版本号说明

此前 `pyproject.toml` 写 0.1.0、`core.__version__` 写 0.3.0，而实际发布的标签已经到
**v0.4.1**——三处互不相干，谁都不对。本版起以 `core.__version__` 为准，
`test_project_meta` 会验证它与 `pyproject.toml` 一致。跳过 0.4.x 直接到 0.5.0，
是为了不与已发布的 v0.4.0 / v0.4.1 标签冲突。

## v0.4.1 及更早

见 Git 历史与《项目方案.md》的实施记录。
